name: cd-deploy

on:
  push:
    branches: [main]
    paths-ignore:
      - 'docs/**'
      - 'README.md'
      - '.gitignore'
  workflow_dispatch:

permissions:
  id-token: write
  contents: read
  actions: write    # to invoke e2e.yml via workflow_call

concurrency:
  group: cd-deploy-dev
  cancel-in-progress: false

jobs:
  # Pass 1: deploy infra with all EG-function subscriptions disabled.
  # ARM validates EG sub endpoints against the production slot's registered
  # functions, which don't exist until publish-functions + swap-slots runs.
  deploy-infra:
    runs-on: ubuntu-24.04
    outputs:
      function_app_name: ${{ steps.outputs.outputs.functionAppName }}
      function_app_hostname: ${{ steps.outputs.outputs.functionAppHostname }}
      enable_archive_sub: ${{ steps.flags.outputs.archive }}
      enable_push_sub: ${{ steps.flags.outputs.push }}
    steps:
      - uses: actions/checkout@v6

      - name: Compute EG-sub flags from project presence
        id: flags
        run: |
          archive=false
          push=false
          [ -d "src/Notify.Archive" ] && archive=true
          [ -d "src/Notify.PushDelivery" ] && push=true
          echo "archive=${archive}" >> "$GITHUB_OUTPUT"
          echo "push=${push}" >> "$GITHUB_OUTPUT"

      - name: Azure login (OIDC)
        uses: azure/login@v3
        with:
          client-id: ${{ vars.AZURE_CLIENT_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}

      - name: Deploy infra (Bicep) — pass 1, EG subs disabled
        id: deploy
        uses: azure/arm-deploy@v2
        with:
          scope: resourcegroup
          resourceGroupName: ${{ vars.AZURE_RG_DEV }}
          template: infra/main.bicep
          parameters: env=dev enableArchiveSubscription=false enablePushSubscription=false
          failOnStdErr: false

      - name: Capture outputs
        id: outputs
        run: |
          NAME="${{ steps.deploy.outputs.functionAppName }}"
          HOST="${{ steps.deploy.outputs.functionAppHostname }}"
          echo "functionAppName=${NAME:-}" >> "$GITHUB_OUTPUT"
          echo "functionAppHostname=${HOST:-}" >> "$GITHUB_OUTPUT"

  publish-functions:
    needs: deploy-infra
    if: needs.deploy-infra.outputs.function_app_name != ''
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix:
        project:
          - Notify.IngestionApi
          - Notify.PushDelivery
          - Notify.Archive
          - Notify.InboxApi
          - Notify.DeviceApi
    steps:
      - uses: actions/checkout@v6

      - name: Set up .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '10.0.x'

      - name: Skip if project does not exist yet (Phase 0)
        id: gate
        run: |
          if [ -d "src/${{ matrix.project }}" ]; then
            echo "exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "exists=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Azure login (OIDC)
        if: steps.gate.outputs.exists == 'true'
        uses: azure/login@v3
        with:
          client-id: ${{ vars.AZURE_CLIENT_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}

      - name: Build ${{ matrix.project }}
        if: steps.gate.outputs.exists == 'true'
        run: |
          set -eo pipefail
          dotnet publish "src/${{ matrix.project }}/${{ matrix.project }}.csproj" \
            -c Release -o ./publish

      # Azure/functions-action handles Linux Consumption + .NET isolated
      # correctly. The older `az functionapp deployment source config-zip`
      # path goes through Kudu which returns 503 on Linux Consumption.
      - name: Publish ${{ matrix.project }} to staging slot
        if: steps.gate.outputs.exists == 'true'
        uses: Azure/functions-action@v1
        with:
          app-name: ${{ needs.deploy-infra.outputs.function_app_name }}
          slot-name: staging
          package: ./publish

  # Promote staging → production so the production slot has the function
  # endpoints registered before pass 2 creates EG subs that target them.
  swap-slots:
    needs: [deploy-infra, publish-functions]
    if: success() && needs.deploy-infra.outputs.function_app_name != ''
    runs-on: ubuntu-24.04
    steps:
      - name: Azure login (OIDC)
        uses: azure/login@v3
        with:
          client-id: ${{ vars.AZURE_CLIENT_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}

      - name: Swap staging → production
        run: |
          az functionapp deployment slot swap \
            -g "${{ vars.AZURE_RG_DEV }}" \
            -n "${{ needs.deploy-infra.outputs.function_app_name }}" \
            --slot staging --target-slot production

  # Pass 2: re-deploy infra with EG subs enabled now that the production slot
  # has the matching function endpoints registered (post-swap). Skipped when
  # neither the archive nor push project exists yet (Phase 0).
  enable-eventgrid-subs:
    needs: [deploy-infra, swap-slots]
    if: |
      success() &&
      (needs.deploy-infra.outputs.enable_archive_sub == 'true' ||
       needs.deploy-infra.outputs.enable_push_sub == 'true')
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6

      - name: Azure login (OIDC)
        uses: azure/login@v3
        with:
          client-id: ${{ vars.AZURE_CLIENT_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}

      - name: Deploy infra (Bicep) — pass 2, EG subs enabled
        uses: azure/arm-deploy@v2
        with:
          scope: resourcegroup
          resourceGroupName: ${{ vars.AZURE_RG_DEV }}
          template: infra/main.bicep
          parameters: >-
            env=dev
            enableArchiveSubscription=${{ needs.deploy-infra.outputs.enable_archive_sub }}
            enablePushSubscription=${{ needs.deploy-infra.outputs.enable_push_sub }}
          failOnStdErr: false

  # E2E runs last, against the production slot, so EG subs (if applicable) are
  # in place and the test exercises the actually-deployed system.
  e2e:
    needs: [deploy-infra, swap-slots, enable-eventgrid-subs]
    if: |
      always() &&
      needs.deploy-infra.result == 'success' &&
      needs.swap-slots.result == 'success' &&
      (needs.enable-eventgrid-subs.result == 'success' || needs.enable-eventgrid-subs.result == 'skipped')
    uses: ./.github/workflows/e2e.yml
    with:
      target_hostname: ${{ needs.deploy-infra.outputs.function_app_hostname }}
      slot: production
    secrets: inherit
