# ── .NET ──────────────────────────────────────────────────────────
bin/
obj/
*.user
*.suo
.vs/
*.dll
*.pdb
*.exe
TestResults/
coverage.*.xml
*.coverage

# Azure Functions local-only config (NEVER commit; contains keys)
local.settings.json
.azure/
.azurefunctions/

# ── Swift / Xcode ────────────────────────────────────────────────
# `app/Notify.xcodeproj` is the source of truth and is tracked.
# Only per-user state inside it is ignored.
xcuserdata/
*.xcuserstate
DerivedData/
.build/
.swiftpm/
Package.resolved
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots/
fastlane/test_output/

# ── macOS / IDE ──────────────────────────────────────────────────
.DS_Store
.idea/
.vscode/*
# Commit shared dev configs
!.vscode/launch.json
!.vscode/tasks.json
!.vscode/.gitkeep

# ── Local dev volumes ────────────────────────────────────────────
.azurite/
.cosmos/
.docker/

# ── Secrets ──────────────────────────────────────────────────────
*.p8
*.p12
*.cer
*.mobileprovision
.env
.env.*
!.env.example
